---
description: Show recent did-you-get-it prompt cleanups (original → cleaned)
---

Run the command below and show the user its output verbatim in a code block,
with a brief one-line intro. `$ARGUMENTS` is an optional count (default 10).

!`bash "${CLAUDE_PLUGIN_ROOT}/hooks/dygi.sh" history $ARGUMENTS`
