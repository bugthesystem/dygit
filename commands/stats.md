---
description: Show did-you-get-it cleanup statistics
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/hooks/dygi.sh:*)"]
---

Cleanup statistics:

```!
"${CLAUDE_PLUGIN_ROOT}/hooks/dygi.sh" stats
```

Show the output above to the user in a code block.
